{
  "name": "datastructures-and-algortihms",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "rm -rf dist && tsc && node dist/app.js --set",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ts-node": "^10.4.0",
    "typescript": "^4.4.2"
  }
}
